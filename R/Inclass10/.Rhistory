data(iris)
names(iris)
head(Sepal.Length, 6)
head(iris$Sepal.Length, 6)
summary(iris$Sepal.Length, 6)
summary(iris$Sepal.Length)
names(iris)
?hclust
?scale
#Clustering for protein data
setwd('C:/Users/Denisa/Google Drive/Fall14/Data Analytics/Notes/week11/Lect30')
protein <- read.table('protein.txt', header=T, sep='\t')
#Always examine data first
names(protein)
head(protein)
summary(protein)
#Scale variables if they are not in the same
#unit of measurement (make them have mean 0 and std =1)
#features we will use to determine distances and clusters
features <- protein[,2:10]
View(features)
View(protein)
View(iris)
features <- iris[,1:4]
View(features)
scaled_features <- scale(features)
View(features)
View(protein)
means <- attr(scaled_features,"scaled:center")
print(means)
stdv <- attr(scaled_features, "scaled:scale")
print(stdv)
distance <- dist(scaled_features, method = "euclidean")
print(distance)
hier_cl <- hclust(distance, method="ward.D")
View(iris)
plot(hier_cl, labels = iris$Species)
rect.hclust(hier_cl, k = 3)
groups <- cutree(hier_cl, k = 3)
print(groups) #each country to what group
View(iris)
View(iris)
?kclust
?hclust
data(iris)
names(iris)
head(iris$Sepal.Length, 6)
summary(iris$Sepal.Length)
head(iris$Sepal.Width, 6)
summary(iris$Sepal.Length)
head(iris$Petal.Length, 6)
summary(iris$Sepal.Length)
head(iris$Petal.Width, 6)
summary(iris$Sepal.Length)
features <- iris[,1:4]
scaled_features <- scale(features)
means <- attr(scaled_features,"scaled:center")
print(means)
stdv <- attr(scaled_features, "scaled:scale")
print(stdv)
distance <- dist(scaled_features, method = "euclidean")
print(distance)
hier_cl <- hclust(distance, method="ward.D")
k_cl <- kmeans(distance)
k_cl <- kmeans(distance, 5, nstart=100, iter.max=100)
k_cl
k_cl$cluster
species_frame = data.frame(iris$species, hier_cl$cluster, k_cl$cluster)
hier_cl$cluster
hier_cl <- hclust(distance, method="ward.D")
hier_cl$cluster
summary(hier_cl)
summary(iris$Sepal.Length)
features <- iris[,1:4]
scaled_features <- scale(features)
means <- attr(scaled_features,"scaled:center")
print(means)
stdv <- attr(scaled_features, "scaled:scale")
print(stdv)
distance <- dist(scaled_features, method = "euclidean")
print(distance)
hier_cl <- hclust(distance, method="ward.D")
plot(hier_cl, labels = iris$Species)
rect.hclust(hier_cl, k = 3)
groups <- cutree(hier_cl, k = 3)
print(groups) #each country to what group
summary(hier_cl)
species_frame = data.frame(iris$species, hier_cl$order, k_cl$cluster)
compare <- cbind(groups,kmeans_clusters$cluster)
compare <- cbind(groups,k_cl$cluster)
compare <- as.data.frame(compare)
names(compare) <- c("Hierarchical", "kmeans")
compare <- cbind(iris$Species,compare)
compare
k_cl <- kmeans(distance, 3, nstart=100, iter.max=100)
compare <- cbind(groups,k_cl$cluster)
compare <- as.data.frame(compare)
names(compare) <- c("Hierarchical", "kmeans")
compare <- cbind(iris$Species,compare)
compare
